[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "CTkPlus"
version = "0.1.0"
description = "vidgets for customtkinter"
dependencies = [
    "customtkinter",
]
