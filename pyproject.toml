[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ctkplus"
version = "0.1.4"
description = "vidgets for customtkinter"
dependencies = [
    "customtkinter",
]
